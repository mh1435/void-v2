name = "void-proxy"
main = "index.js"
compatibility_date = "2025-01-01"

# Secret to set in Cloudflare Dashboard → Workers → void-proxy → Settings → Variables:
#   HF_SPACE_URL  →  https://YOUR_USERNAME-void-core.hf.space
